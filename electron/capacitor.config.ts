import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.faeway.cultivation.idle',
  appName: 'cultivation-idle',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
